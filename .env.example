# GameResearch AI — environment template
# Copy to .env and fill in. NEVER commit a real key.

# NVIDIA NIM API key from https://build.nvidia.com
# (used only via the environment at runtime; never stored or logged)
NVIDIA_API_KEY=

# Local model (offline mode binding point) — GGUF files are NOT committed.
# GAMERES_LOCAL_MODEL=models/qwen3-0.6b-q4_0.gguf
